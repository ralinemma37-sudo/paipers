"use client";

/**
 * Ancien gate top-nav — remplacé par DesktopChromeGate (sidebar + header).
 * Conservé pour imports éventuels ; ne monte plus rien.
 */
export default function DesktopTopNavGate() {
  return null;
}
